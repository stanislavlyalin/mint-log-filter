[package]
name = "mint-log-filter"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.5"
clipboard = { version = "0.5.0", optional = true }

[target.'cfg(target_os = "windows")'.dependencies]
clipboard = "0.5.0"
